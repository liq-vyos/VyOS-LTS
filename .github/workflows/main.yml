name: Build VyOS LTS

env:
  REPO_URL: https://github.com/vyos/vyos-build
  REPO_GIT: https://github.com/vyos/vyos-build.git
  UPLOAD_BIN_DIR: true
  UPLOAD_FIRMWARE: true
  UPLOAD_COWTRANSFER: false
  UPLOAD_WETRANSFER: false
  UPLOAD_RELEASE: true
  DELETE_OLD_WORKFLOWS: true
  DELETE_OLD_RELEASES: true
  
on:
  push:
  schedule:
    - cron: '0 0 * * 0'  # Run build every week

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Build vyos-build docker image
      id: build-docker
      run: |
        git clone -b current --single-branch https://github.com/vyos/vyos-build.git
        cd vyos-build
        docker build -t vyos-builder docker
        run: echo "status=success" >>$GITHUB_OUTPUT
 
    - name: Build iso
      id: build-iso
      if: (!cancelled()) && steps.build-docker.outputs.status == 'success'
      run: |
        [ "$(basename "$PWD")" != "vyos-build" ] && cd vyos-build
        ls -al; pwd
        docker run --rm --privileged -v $(pwd)/vyos-build:/vyos -w /vyos vyos-builder bash -c './configure --architecture amd64 --build-type release && sudo make iso'
        #echo "::set-output name=status::success"
        run: echo "status=success" >>$GITHUB_OUTPUT
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV
 
    - name: Check space usage
      if: (!cancelled()) && steps.build-iso.outputs.status == 'success'
      run: pwd; ls -al; df -hT
 
    - name: Persist created iso
      id: persist-iso
      uses: actions/upload-artifact@main
      if: (!cancelled()) && steps.build-iso.outputs.status == 'success' && env.UPLOAD_BIN_DIR == 'true'
      with:
        name: vyos-lts_${{ env.FILE_DATE }}
        path: 'vyos-build/build/live-image-amd64.hybrid.iso'
      run: echo "status=success" >>$GITHUB_OUTPUT

    - name: Generate release tag
      id: tag
      if: (!cancelled()) && steps.build-iso.outputs.status == 'success' && env.UPLOAD_RELEASE == 'true'
      run: |
        #echo "::set-output name=release_tag::$(date +"%Y.%m.%d-%H%M")"
        echo "release_tag=_$(date +"%Y.%m.%d-%H%M")" >> $GITHUB_ENV
        touch release.txt
        [ $UPLOAD_COWTRANSFER = true ] && echo "🔗 [Cowtransfer](${{ steps.cowtransfer.outputs.url }})" >> release.txt
        [ $UPLOAD_WETRANSFER = true ] && echo "🔗 [WeTransfer](${{ steps.wetransfer.outputs.url }})" >> release.txt
        echo "status=success" >>$GITHUB_OUTPUT

    - name: Upload firmware to release
      uses: softprops/action-gh-release@v1
      if: (!cancelled()) && steps.tag.outputs.status == 'success' && env.UPLOAD_RELEASE == 'true'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      with:
        tag_name: ${{ steps.tag.outputs.release_tag }}
        body_path: release.txt
        files: vyos-build/build/*

    - name: Delete workflow runs
      uses: GitRML/delete-workflow-runs@main
      if: env.DELETE_OLD_WORKFLOWS == 'true' && !cancelled()
      with:
        retain_days: 1
        keep_minimum_runs: 3

    - name: Remove old Releases
      uses: dev-drprasad/delete-older-releases@v0.1.0
      if: env.DELETE_OLD_RELEASES == 'true' && !cancelled()
      with:
        keep_latest: 3
        delete_tags: true
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}   
